@page
@model PostApplication.Pages.Couries.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Courier[0].CourierFirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Courier[0].CourierLastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Courier[0].PhoneNumber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Courier) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CourierFirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CourierLastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhoneNumber)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
