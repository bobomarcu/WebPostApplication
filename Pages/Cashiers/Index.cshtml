@page
@model PostApplication.Pages.Cashiers.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cashier[0].CashierFirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cashier[0].CashierLastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cashier[0].PhoneNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cashier[0].PostOffice)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Cashier) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CashierFirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CashierLastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhoneNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PostOffice.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
